# Test Configuration
spring.profiles.active=test

# MongoDB Embedded
spring.data.mongodb.uri=mongodb://localhost:27017/vuln-scan-test
spring.data.mongodb.auto-index-creation=true

# NVD API (Mock)
nvd.api.url=http://localhost:8080/mock-nvd
nvd.api.key=test-api-key

# AI Providers (Disabled for tests)
ai.openai.enabled=false
ai.claude.enabled=false
ai.gemini.enabled=false
ai.fallback.enabled=true

# Logging
logging.level.com.mercadolibre.vulnscania=DEBUG
logging.level.org.springframework.data.mongodb=WARN
logging.level.org.mongodb=WARN

